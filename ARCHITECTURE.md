# 🏗️ CrowdSight Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Landing (/)  │  │ Login/Signup │  │  Dashboard   │          │
│  │              │  │   Forms      │  │  (Protected) │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
│  TailwindCSS │ React Hook Form │ Axios │ localStorage           │
└────────────────────────────┬──────────────────────────────────┘
                             │
                             │ HTTP Requests
                             │ Authorization: Bearer <JWT>
                             │
┌────────────────────────────▼──────────────────────────────────┐
│                    NEXT.JS FRONTEND                            │
│                    (Port 3000)                                 │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  pages/                                                  │  │
│  │  ├── index.js         → Landing page                    │  │
│  │  ├── login.js         → Login form                      │  │
│  │  ├── signup.js        → Registration form               │  │
│  │  └── dashboard.js     → Protected dashboard             │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  styles/globals.css   → TailwindCSS + Custom Styles     │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Environment: NEXT_PUBLIC_API_URL                              │
└────────────────────────────┬──────────────────────────────────┘
                             │
                             │ API Calls (axios)
                             │ POST /api/auth/signup
                             │ POST /api/auth/login
                             │ GET /api/auth/me
                             │
┌────────────────────────────▼──────────────────────────────────┐
│                    EXPRESS BACKEND                             │
│                    (Port 5000)                                 │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  Middleware Stack                                        │  │
│  │  ├── CORS          → Enable cross-origin requests       │  │
│  │  ├── express.json  → Parse JSON bodies                  │  │
│  │  └── dotenv        → Load environment variables         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  routes/auth.js                                          │  │
│  │  ├── POST /api/auth/signup    (public)                  │  │
│  │  ├── POST /api/auth/login     (public)                  │  │
│  │  └── GET  /api/auth/me        (protected)               │  │
│  └───────────────────┬─────────────────────────────────────┘  │
│                      │                                          │
│  ┌───────────────────▼─────────────────────────────────────┐  │
│  │  middleware/authMiddleware.js                           │  │
│  │  ├── Verify JWT token                                   │  │
│  │  ├── Decode user data                                   │  │
│  │  └── Attach userId to request                           │  │
│  └───────────────────┬─────────────────────────────────────┘  │
│                      │                                          │
│  ┌───────────────────▼─────────────────────────────────────┐  │
│  │  controllers/authController.js                          │  │
│  │  ├── signup()   → Hash password, create user, sign JWT │  │
│  │  ├── login()    → Verify password, sign JWT            │  │
│  │  └── getMe()    → Return current user data             │  │
│  └───────────────────┬─────────────────────────────────────┘  │
│                      │                                          │
│                      │ Prisma ORM                               │
│                      │                                          │
│  Environment: DATABASE_URL, JWT_SECRET, PORT                   │
└────────────────────────────┬──────────────────────────────────┘
                             │
                             │ SQL Queries
                             │ (Type-safe with Prisma)
                             │
┌────────────────────────────▼──────────────────────────────────┐
│                    PRISMA ORM                                  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  prisma/schema.prisma                                    │  │
│  │                                                           │  │
│  │  model User {                                            │  │
│  │    id        String   @id @default(cuid())              │  │
│  │    name      String                                      │  │
│  │    email     String   @unique                            │  │
│  │    password  String   // bcrypt hashed                   │  │
│  │    createdAt DateTime @default(now())                    │  │
│  │    updatedAt DateTime @updatedAt                         │  │
│  │  }                                                        │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Prisma Client → Type-safe database queries                   │
└────────────────────────────┬──────────────────────────────────┘
                             │
                             │ MySQL Protocol
                             │ SSL/TLS Encrypted
                             │
┌────────────────────────────▼──────────────────────────────────┐
│                    PLANETSCALE DATABASE                        │
│                    (MySQL Compatible)                          │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  Table: users                                            │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ id (PK)  │ name     │ email (UQ) │ password │ ... │ │  │
│  │  ├──────────┼──────────┼────────────┼──────────┼─────┤ │  │
│  │  │ clx...   │ John Doe │ john@ex... │ $2b$10...│ ... │ │  │
│  │  │ cly...   │ Jane Doe │ jane@ex... │ $2b$10...│ ... │ │  │
│  │  └──────────┴──────────┴────────────┴──────────┴─────┘ │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Features: 5GB Storage, 1B reads/month, Auto-scaling           │
└─────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow

```
┌─────────┐
│  USER   │
└────┬────┘
     │
     │ 1. Enter credentials (email, password)
     ▼
┌─────────────────┐
│ Signup/Login    │
│   Form          │
└────┬────────────┘
     │
     │ 2. Submit via axios
     ▼
┌─────────────────┐
│   Frontend      │ 3. Validate input (react-hook-form)
│   (Next.js)     │
└────┬────────────┘
     │
     │ 4. POST /api/auth/signup or /login
     │    { email, password, name }
     ▼
┌─────────────────────────────────┐
│   Backend (Express)             │
│                                  │
│   authController.signup()       │ 5. Validate email format
│   or login()                     │ 6. Check if user exists
└────┬────────────────────────────┘
     │
     │ SIGNUP PATH                │ LOGIN PATH
     ▼                            │
┌─────────────────┐               │
│  bcrypt.hash()  │ 7. Hash pwd   │
└────┬────────────┘               │
     │                            │
     │ 8. Create user             │ 8. Find user
     ▼                            ▼
┌─────────────────────────────────────┐
│   Prisma Client                     │
│   prisma.user.create() / findUnique()│
└────┬────────────────────────────────┘
     │
     │ 9. Execute SQL query
     ▼
┌─────────────────┐
│  PlanetScale DB │ 10. INSERT / SELECT
└────┬────────────┘
     │
     │ 11. Return user data
     ▼
┌─────────────────────────┐
│   Backend               │
│   jwt.sign()            │ 12. Generate JWT token
└────┬────────────────────┘    { userId, email }
     │                          Secret: JWT_SECRET
     │                          Expires: 7 days
     │
     │ 13. Return { user, token }
     ▼
┌─────────────────┐
│   Frontend      │ 14. Store in localStorage
│                 │     localStorage.setItem('token', token)
└────┬────────────┘
     │
     │ 15. Redirect to /dashboard
     ▼
┌─────────────────────────┐
│   Dashboard Page        │
│   useEffect(() => {     │ 16. Verify token
│     verifyAuth()        │
│   })                    │
└────┬────────────────────┘
     │
     │ 17. GET /api/auth/me
     │     Authorization: Bearer <token>
     ▼
┌─────────────────────────┐
│   authMiddleware        │ 18. Verify JWT
│   jwt.verify(token)     │ 19. Decode userId
└────┬────────────────────┘
     │
     │ 20. req.userId = decoded.userId
     ▼
┌─────────────────────────┐
│   authController.getMe()│ 21. Fetch user from DB
└────┬────────────────────┘
     │
     │ 22. Return user data
     ▼
┌─────────────────┐
│   Dashboard     │ 23. Display user info
│   Shows: name,  │     Show protected content
│   email, stats  │
└─────────────────┘
```

---

## Data Flow: Signup Example

```
User Input:
{
  name: "John Doe",
  email: "john@example.com",
  password: "password123"
}
    │
    ▼
Frontend Validation (react-hook-form):
- Name: min 2 chars ✓
- Email: valid format ✓
- Password: min 6 chars ✓
    │
    ▼
axios.post('/api/auth/signup', data)
    │
    ▼
Backend Validation:
- All fields present ✓
- Email format ✓
- Email unique ✓
    │
    ▼
bcrypt.hash("password123", 10)
→ "$2b$10$abc...xyz"
    │
    ▼
prisma.user.create({
  name: "John Doe",
  email: "john@example.com",
  password: "$2b$10$abc...xyz"
})
    │
    ▼
MySQL INSERT INTO users...
    │
    ▼
Return user:
{
  id: "clx123abc...",
  name: "John Doe",
  email: "john@example.com",
  createdAt: "2025-11-03T..."
}
    │
    ▼
jwt.sign({ userId: "clx123abc...", email: "john@..." })
→ "eyJhbGciOiJIUzI1NiIsInR5..."
    │
    ▼
Response to frontend:
{
  message: "User created successfully",
  user: { id, name, email, createdAt },
  token: "eyJhbGciOiJIUzI1NiIsInR5..."
}
    │
    ▼
localStorage.setItem('token', token)
    │
    ▼
router.push('/dashboard')
```

---

## Technology Stack

```
┌─────────────────────────────────────┐
│         FRONTEND LAYER              │
│  ┌──────────────────────────────┐   │
│  │ Next.js 14                   │   │ ← React Framework
│  │ React 18                     │   │ ← UI Library
│  │ TailwindCSS 3                │   │ ← Styling
│  │ Axios                        │   │ ← HTTP Client
│  │ react-hook-form              │   │ ← Form Validation
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
             │
             │ REST API (JSON)
             ▼
┌─────────────────────────────────────┐
│         BACKEND LAYER               │
│  ┌──────────────────────────────┐   │
│  │ Node.js 18+                  │   │ ← Runtime
│  │ Express.js 4                 │   │ ← Web Framework
│  │ jsonwebtoken                 │   │ ← JWT Auth
│  │ bcrypt                       │   │ ← Password Hashing
│  │ CORS                         │   │ ← Cross-Origin
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
             │
             │ Prisma Client
             ▼
┌─────────────────────────────────────┐
│         ORM LAYER                   │
│  ┌──────────────────────────────┐   │
│  │ Prisma ORM                   │   │ ← Type-safe queries
│  │ @prisma/client               │   │ ← Generated client
│  │ Prisma Schema                │   │ ← Model definitions
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
             │
             │ MySQL Protocol
             ▼
┌─────────────────────────────────────┐
│         DATABASE LAYER              │
│  ┌──────────────────────────────┐   │
│  │ PlanetScale MySQL            │   │ ← Serverless MySQL
│  │ 5GB Storage (Free)           │   │ ← Cloud Database
│  │ Auto-scaling                 │   │ ← Scalable
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────┐
│              USERS                          │
│         (Global Access)                     │
└───────────────────┬─────────────────────────┘
                    │
                    │ HTTPS
                    ▼
┌─────────────────────────────────────────────┐
│          VERCEL CDN                         │
│   ┌─────────────────────────────────┐       │
│   │  Next.js Frontend               │       │
│   │  - Static files                 │       │
│   │  - Server-side rendering        │       │
│   │  - Edge functions               │       │
│   └──────────────┬──────────────────┘       │
│   Region: Global │                          │
└──────────────────┼──────────────────────────┘
                   │
                   │ API Calls
                   ▼
┌─────────────────────────────────────────────┐
│          RAILWAY                            │
│   ┌─────────────────────────────────┐       │
│   │  Express Backend                │       │
│   │  - REST API                     │       │
│   │  - JWT Authentication           │       │
│   │  - Business Logic               │       │
│   └──────────────┬──────────────────┘       │
│   Region: US     │                          │
└──────────────────┼──────────────────────────┘
                   │
                   │ Prisma Client
                   ▼
┌─────────────────────────────────────────────┐
│          PLANETSCALE                        │
│   ┌─────────────────────────────────┐       │
│   │  MySQL Database                 │       │
│   │  - User data                    │       │
│   │  - Auto-scaling                 │       │
│   │  - Daily backups                │       │
│   └─────────────────────────────────┘       │
│   Region: AWS (Multi-region)                │
└─────────────────────────────────────────────┘
```

---

## Security Layers

```
┌────────────────────────────────────┐
│  CLIENT SIDE                       │
│  ├─ Form Validation               │  ← react-hook-form
│  ├─ HTTPS Only                    │  ← Vercel SSL
│  └─ Token Storage (localStorage)  │  ← JWT stored locally
└────────────────┬───────────────────┘
                 │
                 │ Encrypted Connection
                 ▼
┌────────────────────────────────────┐
│  SERVER SIDE                       │
│  ├─ CORS Protection               │  ← Allowed origins only
│  ├─ JWT Verification              │  ← jsonwebtoken
│  ├─ Password Hashing (bcrypt)     │  ← 10 salt rounds
│  ├─ Input Validation              │  ← Email, password checks
│  └─ Environment Variables         │  ← Secrets not in code
└────────────────┬───────────────────┘
                 │
                 │ Encrypted Connection
                 ▼
┌────────────────────────────────────┐
│  DATABASE                          │
│  ├─ SSL/TLS Required              │  ← PlanetScale SSL
│  ├─ Unique Constraints            │  ← Email uniqueness
│  ├─ Connection Pooling            │  ← Prisma manages
│  └─ Automatic Backups             │  ← PlanetScale
└────────────────────────────────────┘
```

---

## File Relationships

```
CrowdSight/
│
├─ frontend/pages/index.js ──────► Landing page
│                                   ├─ Button → /signup
│                                   └─ Button → /login
│
├─ frontend/pages/signup.js ─────► Registration
│      │                            ├─ Form validation
│      └─ axios.post() ────────────┼─► backend/routes/auth.js
│                                   │   POST /api/auth/signup
│                                   └─► backend/controllers/authController.js
│                                       signup() function
│                                       ├─ bcrypt.hash()
│                                       ├─ prisma.user.create()
│                                       └─ jwt.sign()
│
├─ frontend/pages/login.js ──────► Login
│      │
│      └─ axios.post() ────────────┼─► backend/routes/auth.js
│                                   │   POST /api/auth/login
│                                   └─► backend/controllers/authController.js
│                                       login() function
│                                       ├─ prisma.user.findUnique()
│                                       ├─ bcrypt.compare()
│                                       └─ jwt.sign()
│
└─ frontend/pages/dashboard.js ──► Protected page
       │
       └─ axios.get() ─────────────┼─► backend/routes/auth.js
                                   │   GET /api/auth/me
                                   ├─► backend/middleware/authMiddleware.js
                                   │   jwt.verify()
                                   └─► backend/controllers/authController.js
                                       getMe() function
                                       └─ prisma.user.findUnique()
```

---

**This architecture diagram shows how all components work together! 🏗️**
